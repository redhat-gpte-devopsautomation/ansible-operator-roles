kind: Service
apiVersion: v1
metadata:
  name: "{{ nexus_ocp_name }}"
  namespace: "{{ nexus_ocp_namespace }}"
  labels:
    app: "{{ nexus_ocp_name }}"
spec:
  selector:
    app: "{{ nexus_ocp_name }}"
  ports:
  - name: "nexus-tcp"
    port: 8081
    protocol: TCP
    targetPort: 8081
  sessionAffinity: None
  type: ClusterIP
