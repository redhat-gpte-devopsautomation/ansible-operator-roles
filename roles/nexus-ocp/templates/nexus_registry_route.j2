kind: Route
apiVersion: v1
metadata:
  name: "{{ _nexus_name }}-registry"
  namespace: "{{ _nexus_namespace }}"
  labels:
    app: "{{ _nexus_name }}"
spec:
  to:
    kind: Service
    name: "{{ _nexus_name }}-registry"
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
